# vim: set filetype=gitconfig : ~
# vim: set shiftwidth=4 : ~
# vim: set softtabstop=0 : ~
# vim: set noexpandtab : ~

[alias]
	# ========== [ DIFF ] ==========
	di = diff
	dt = difftool
	dc = diff --cached
	ds = diff --stat --find-copies
	dn = diff --numstat
	dh = diff --shortstat

