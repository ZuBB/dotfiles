#!/usr/bin/env bash

# SYNC TASKS

# clear console
console_clear;

# remove trash from history
cleanup_history;

sleep 5s

# ASYNC TASKS

if [ -f ~/.local/bin/async_logout_tasks ]; then
    (nohup ~/.local/bin/async_logout_tasks >/dev/null 2>&1 &) >/dev/null 2>&1
fi

